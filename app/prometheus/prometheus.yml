global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "bot"
    static_configs:
      - targets: ["bot:9000"]
    labels:
      app: "bot"

  - job_name: "scrapper"
    static_configs:
      - targets: ["scrapper:9001"]
    labels:
      app: "scrapper"
